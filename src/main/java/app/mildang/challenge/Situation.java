package app.mildang.challenge;

/**
 * 설문 3번 «밀가루를 가장 많이 먹는 상황은 언제인가요?» (화면 온보딩_03).
 *
 * ⚠ 예산 산식에는 반영하지 않는다 — 전부 가중치 1.0 (팀 결정 2026-08-17).
 * 받아서 저장만 해두는 이유는 나중에 AI(흥정 어조·대시보드 팁·리포트 발견)가 쓰기 위해서다.
 * 예산에 곱하기 시작하면 그때 여기에 배수를 붙이면 된다.
 */
public enum Situation {
    MEAL,        // 식사
    SNACK,       // 간식
    LATE_NIGHT,  // 야식
    IRREGULAR    // 불규칙
}
